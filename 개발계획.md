# 대학 수강신청 설문조사 웹 개발 계획서

## 프로젝트 개요

학생들의 실제 수강 의향을 수집하여 시간표 알고리즘에 활용하기 위한 설문조사 웹 애플리케이션 개발

### 목표
- 학생들이 다음 학기 수강 계획을 우선순위별로 입력
- 교수별 선호도 및 대체 강의 선택 기능 제공
- 현실적이고 정확한 수강 수요 데이터 수집

## 기술 스택

### 프론트엔드
- **HTML5/CSS3/JavaScript (Vanilla JS)**
  - 외부 프레임워크 없이 순수 JavaScript로 구현
  - 군더더기 없는 깔끔한 UI (학교 수강신청 시스템 스타일 참고)
  - 반응형 디자인으로 PC 환경 최적화

### 백엔드
- **Go (Golang) 1.21+**
  - 단일 바이너리 컴파일로 배포 용이
  - 고성능 웹 서버
  - `embed` 패키지로 정적 파일 내장

### 데이터베이스
- **SQLite**
  - 파일 기반 DB로 별도 서버 불필요
  - 단일 `.db` 파일로 모든 데이터 관리
  - 이식 및 백업 용이
  - Go에서 `mattn/go-sqlite3` 드라이버 사용

### 배포 방식
- **단일 실행 파일 배포**
  - Go 바이너리에 HTML/CSS/JS 및 SQLite DB 내장
  - 교수님 서버에서 단순히 실행 파일만 실행하면 됨
  - 크로스 플랫폼 지원 (Linux/macOS/Windows)
  - 포트 설정만으로 즉시 서비스 가능

## 시스템 아키텍처

```
┌─────────────────────────────────────────┐
│         단일 Go 실행 파일                │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │  내장 정적 파일 (embed)            │  │
│  │  - HTML, CSS, JavaScript         │  │
│  │  - 개설강좌.csv                   │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  Go 웹 서버 (net/http)            │  │
│  │  - RESTful API                   │  │
│  │  - 정적 파일 서빙                 │  │
│  │  - 세션 관리                      │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  비즈니스 로직                     │  │
│  │  - 로그인/인증                    │  │
│  │  - 과목 검색/필터링               │  │
│  │  - 수강 선택 관리                 │  │
│  │  - 학점 제한 검증                 │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  SQLite 데이터베이스               │  │
│  │  - 학생 정보                      │  │
│  │  - 개설 강좌                      │  │
│  │  - 수강 선택 데이터               │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## 데이터베이스 설계

### 테이블 구조

#### 1. students (학생 정보)
```sql
CREATE TABLE students (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id VARCHAR(20) UNIQUE NOT NULL,      -- 학번
    name VARCHAR(50) NOT NULL,                   -- 성함
    password VARCHAR(255) NOT NULL,              -- 비밀번호 (해시)
    major VARCHAR(100),                          -- 목표 전공
    minor VARCHAR(100),                          -- 부전공/복수전공
    current_year VARCHAR(10),                    -- 재학 학년 (1-1, 2-2 등)
    special_notes TEXT,                          -- 특이사항
    is_submitted BOOLEAN DEFAULT FALSE,          -- 제출 완료 여부
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 2. courses (개설 강좌)
```sql
CREATE TABLE courses (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    course_code VARCHAR(20) NOT NULL,            -- 과목번호 (CR501-01)
    course_name VARCHAR(200) NOT NULL,           -- 교과목명
    professor VARCHAR(100) NOT NULL,             -- 담당교수
    division VARCHAR(50),                        -- 이수구분 (전공필수, 교양 등)
    field VARCHAR(100),                          -- 교과분야
    area VARCHAR(100),                           -- 교과영역
    credits DECIMAL(2,1),                        -- 학점
    syllabus_link TEXT,                          -- 강의계획서 링크
    schedule TEXT,                               -- 시간표 정보
    UNIQUE(course_code, professor)
);
```

#### 3. course_selections (수강 선택)
```sql
CREATE TABLE course_selections (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id INTEGER NOT NULL,                 -- students.id 참조
    course_id INTEGER NOT NULL,                  -- courses.id 참조
    priority INTEGER NOT NULL,                   -- 우선순위 (1~7지망)
    professor_1st VARCHAR(100),                  -- 교수 1지망
    professor_2nd VARCHAR(100),                  -- 교수 2지망
    professor_3rd VARCHAR(100),                  -- 교수 3지망
    is_alternative BOOLEAN DEFAULT FALSE,        -- 대체 강의 여부
    parent_selection_id INTEGER,                 -- 대체 강의인 경우 원본 선택 참조
    alternative_priority INTEGER,                -- 대체 강의 우선순위 (1~2)
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (student_id) REFERENCES students(id),
    FOREIGN KEY (course_id) REFERENCES courses(id),
    FOREIGN KEY (parent_selection_id) REFERENCES course_selections(id)
);
```

## API 설계

### 인증 API

#### POST /api/login
로그인 (최초 로그인 시 계정 생성)
```json
// Request
{
    "student_id": "2021123456",
    "name": "김민성",
    "password": "mypassword",
    "major": "컴퓨터공학",
    "minor": "경영학",
    "current_year": "3-1",
    "special_notes": "초과학점 신청"
}

// Response
{
    "success": true,
    "is_new_user": false,
    "student": {
        "id": 1,
        "student_id": "2021123456",
        "name": "김민성",
        "is_submitted": false
    },
    "token": "session_token_here"
}
```

#### GET /api/me
현재 로그인한 학생 정보 조회
```json
// Response
{
    "id": 1,
    "student_id": "2021123456",
    "name": "김민성",
    "major": "컴퓨터공학",
    "minor": "경영학",
    "current_year": "3-1",
    "is_submitted": false
}
```

### 강좌 조회 API

#### GET /api/courses?division=전공필수&field=기계공학&search=알고리즘
개설 강좌 목록 조회 (필터링/검색)
```json
// Response
{
    "total": 150,
    "courses": [
        {
            "id": 1,
            "no": 1,
            "course_code": "CR501-01",
            "course_name": "크리에이팅",
            "professor": "목지수",
            "division": "기초학부",
            "field": "기초공학",
            "area": "컴퓨터공학",
            "credits": 3.0,
            "syllabus_link": "",
            "schedule": "화18:00-19:30(E7-L21), 목18:00-19:30(E7-L21)"
        }
    ]
}
```

#### GET /api/courses/:id
특정 강좌 상세 정보
```json
// Response
{
    "id": 1,
    "course_code": "CR501-01",
    "course_name": "크리에이팅",
    "professor": "목지수",
    "division": "기초학부",
    "credits": 3.0,
    "available_professors": ["목지수", "김철수", "이영희"]
}
```

### 수강 선택 API

#### GET /api/selections
내 수강 선택 목록 조회
```json
// Response
{
    "total_credits": 15.0,
    "selections": [
        {
            "id": 1,
            "priority": 1,
            "course": {
                "course_name": "알고리즘",
                "credits": 3.0
            },
            "professor_1st": "김교수",
            "professor_2nd": "이교수",
            "professor_3rd": "박교수",
            "alternatives": [
                {
                    "id": 2,
                    "alternative_priority": 1,
                    "course": {
                        "course_name": "자료구조",
                        "credits": 3.0
                    },
                    "professor_1st": "최교수"
                }
            ]
        }
    ]
}
```

#### POST /api/selections
수강 선택 추가
```json
// Request
{
    "course_id": 1,
    "priority": 1,
    "professor_1st": "김교수",
    "professor_2nd": "이교수",
    "professor_3rd": "박교수"
}

// Response
{
    "success": true,
    "selection_id": 1,
    "total_credits": 3.0
}
```

#### POST /api/selections/:id/alternatives
대체 강의 추가
```json
// Request
{
    "course_id": 5,
    "alternative_priority": 1,
    "professor_1st": "최교수",
    "professor_2nd": "정교수"
}

// Response
{
    "success": true,
    "alternative_id": 2
}
```

#### PUT /api/selections/:id
수강 선택 수정
```json
// Request
{
    "professor_1st": "박교수",
    "professor_2nd": "김교수"
}

// Response
{
    "success": true
}
```

#### DELETE /api/selections/:id
수강 선택 삭제
```json
// Response
{
    "success": true,
    "total_credits": 12.0
}
```

### 제출 API

#### POST /api/submit
설문조사 제출
```json
// Request
{}

// Response
{
    "success": true,
    "message": "설문조사가 제출되었습니다.",
    "total_credits": 15.0,
    "submitted_at": "2026-05-05T10:30:00Z"
}
```

#### POST /api/reopen
설문조사 재수정
```json
// Response
{
    "success": true,
    "message": "설문조사를 다시 수정할 수 있습니다."
}
```

## 화면 구성

### 1. 로그인 페이지 (`/login.html`)
- 학번 입력
- 성함 입력
- 비밀번호 입력
- 최초 로그인 시 추가 정보 입력
  - 목표 전공/부전공/복수전공 선택
  - 재학 학년 (1-1, 2-2 등)
  - 특이사항 (초과학점, 졸업학기 등)

### 2. 메인 페이지 (`/index.html`)
#### 상단: 학생 정보 표시 (최소화)
- 학번, 이름, 목표 전공/부전공
- 로그아웃 버튼

#### 좌측/상단: 개설 강좌 리스트
- 검색/필터 기능
  - 이수구분: 교양, 전공필수, 전공선택, 기초필수, 기초선택
  - 교과분야
  - 교과목명 검색
- 테이블 컬럼
  - NO
  - 과목번호
  - 교과목명
  - 강의계획서 (링크)
  - 담당교수
  - 이수구분
  - 교과분야
  - 교과영역
  - 학점
  - 선택 버튼

#### 하단: 나의 교과목 선택 리스트
- 우선순위별 표시 (1~7지망)
- 각 지망별 표시 정보:
  - 과목명, 교수님 1지망
  - 교수님 2지망 (선택)
  - 교수님 3지망 (선택)
  - 대체 강의 1지망 (선택)
  - 대체 강의 2지망 (선택)
- 색상 구분:
  - 선택 강의: 기본 색
  - 대체 강의 1지망: 밝은 회색
  - 대체 강의 2지망: 더 밝은 회색
- 수정/삭제 기능
- 총 학점 표시

#### 하단: 설문조사 완료 버튼
- 학점 제한 검증 (10~21학점)
- 제출 시 확인 팝업

### 3. 제출 완료 페이지 (`/result.html`)
- 학생 정보 표시
- 제출된 교과목 선택 리스트 (읽기 전용)
- 설문조사 완료 여부 표시
- 수정 가능 기간 안내
- 수정하기 버튼 (메인 페이지로 이동)

### 팝업 모달

#### 1. 과목 선택 팝업
- 몇 지망으로 선택할지 입력 (1~7)
- 현재 나의 교과목 선택 리스트 미리보기
- 교수님 지망 선택 (1, 2, 3지망)
- 대체 강의 선택 옵션
  - 스킵 가능 (나중에 추가)
- 확인/취소

#### 2. 대체 강의 선택 팝업
- 같은 분야 강의 우선 추천
- 대체 강의 우선순위 (1~2지망)
- 교수님 지망 선택
- 안내 메시지: "대체 강의는 원 강의보다 우선순위가 높습니다"

#### 3. 수정 확인 팝업
- 기존 강의를 대체할지 / 대체 강의로 추가할지 선택
- 이미 대체 강의가 있는 경우 어느 것을 삭제할지 선택

## 프로젝트 디렉토리 구조

```
시간표알고리즘/
├── cmd/
│   └── server/
│       └── main.go                 # 메인 실행 파일
├── internal/
│   ├── api/
│   │   ├── auth.go                # 인증 API 핸들러
│   │   ├── courses.go             # 강좌 조회 API
│   │   └── selections.go          # 수강 선택 API
│   ├── db/
│   │   ├── database.go            # DB 연결 및 초기화
│   │   ├── migrations.go          # 테이블 생성 스크립트
│   │   └── queries.go             # SQL 쿼리 함수들
│   ├── models/
│   │   ├── student.go             # 학생 모델
│   │   ├── course.go              # 강좌 모델
│   │   └── selection.go           # 수강 선택 모델
│   ├── middleware/
│   │   ├── auth.go                # 인증 미들웨어
│   │   └── cors.go                # CORS 미들웨어
│   └── util/
│       ├── csv.go                 # CSV 파싱 유틸
│       └── password.go            # 비밀번호 해싱
├── web/
│   ├── static/
│   │   ├── css/
│   │   │   └── style.css          # 메인 스타일시트
│   │   └── js/
│   │       ├── common.js          # 공통 유틸리티
│   │       ├── login.js           # 로그인 페이지 스크립트
│   │       ├── main.js            # 메인 페이지 스크립트
│   │       └── result.js          # 결과 페이지 스크립트
│   └── templates/
│       ├── login.html             # 로그인 페이지
│       ├── index.html             # 메인 페이지
│       └── result.html            # 제출 완료 페이지
├── reference/
│   ├── 개설강좌.csv                # 강좌 데이터
│   ├── 학생용프론트_기능기획서.pdf
│   └── design/
│       ├── 메인페이지.png
│       └── 팝업.png
├── go.mod                         # Go 모듈 정의
├── go.sum                         # Go 의존성 체크섬
├── Makefile                       # 빌드 스크립트
└── README.md                      # 프로젝트 설명서
```

## 개발 단계

### Phase 1: 프로젝트 초기 설정 (1일)
- [ ] Go 프로젝트 초기화
- [ ] 디렉토리 구조 생성
- [ ] 필요한 Go 패키지 설치
  - `github.com/mattn/go-sqlite3` (SQLite)
  - `golang.org/x/crypto/bcrypt` (비밀번호 해싱)
- [ ] SQLite 데이터베이스 스키마 작성
- [ ] CSV 데이터 파싱 및 DB 초기화

### Phase 2: 백엔드 API 개발 (2-3일)
- [ ] DB 연결 및 모델 정의
- [ ] 인증 API 구현
  - 로그인/회원가입 통합
  - 세션 관리 (쿠키 기반)
  - 비밀번호 해싱
- [ ] 강좌 조회 API 구현
  - 전체 목록 조회
  - 필터링/검색 기능
  - 교수 목록 조회
- [ ] 수강 선택 API 구현
  - CRUD 기능
  - 대체 강의 관리
  - 학점 계산 및 검증
- [ ] 제출 API 구현
  - 학점 제한 검증 (10~21점)
  - 제출 상태 관리

### Phase 3: 프론트엔드 개발 (3-4일)
- [ ] HTML 페이지 구조 작성
  - 로그인 페이지
  - 메인 페이지
  - 제출 완료 페이지
- [ ] CSS 스타일링
  - 학교 수강신청 시스템 참고
  - 깔끔하고 포멀한 디자인
  - PC 환경 최적화
- [ ] JavaScript 기능 구현
  - API 통신 (Fetch API)
  - 동적 테이블 렌더링
  - 검색/필터링 UI
  - 모달 팝업 관리
  - 폼 검증
  - 우선순위 관리
  - 대체 강의 선택 로직

### Phase 4: 통합 및 테스트 (1-2일)
- [ ] 프론트엔드-백엔드 통합
- [ ] 전체 플로우 테스트
  - 로그인/회원가입
  - 강좌 검색 및 선택
  - 교수 지망 선택
  - 대체 강의 추가
  - 수정/삭제
  - 제출 및 재수정
- [ ] 예외 상황 처리
  - 중복 선택 방지
  - 학점 초과 경고
  - 네트워크 오류 처리

### Phase 5: 배포 준비 (1일)
- [ ] Go embed로 정적 파일 내장
- [ ] 크로스 플랫폼 빌드
  - Linux (amd64, arm64)
  - macOS (amd64, arm64)
  - Windows (amd64)
- [ ] 실행 스크립트 작성
- [ ] 사용 설명서 작성
- [ ] 교수님 서버 이식 가이드 작성

## 빌드 및 실행

### 개발 환경 실행
```bash
# 의존성 설치
go mod download

# 개발 서버 실행
go run cmd/server/main.go

# 브라우저에서 접속
# http://localhost:8080
```

### 프로덕션 빌드
```bash
# 단일 바이너리 빌드 (현재 OS)
make build

# 모든 플랫폼 빌드
make build-all

# 생성된 파일
# - dist/survey-linux-amd64
# - dist/survey-darwin-amd64
# - dist/survey-windows-amd64.exe
```

### 교수님 서버 배포
```bash
# 1. 실행 파일 업로드
scp dist/survey-linux-amd64 professor-server:/path/to/app/

# 2. 서버에서 실행
ssh professor-server
cd /path/to/app/
chmod +x survey-linux-amd64
./survey-linux-amd64 --port 8080

# 3. 브라우저 접속
# http://server-ip:8080
```

## 주요 기능 구현 세부사항

### 1. 우선순위 관리
- 1~7지망까지 선택 가능
- 같은 우선순위 중복 불가
- 우선순위 변경 시 자동 재정렬
- 선택된 강의는 다른 지망에서 선택 불가

### 2. 교수 지망 선택
- 1지망: 선택한 교수로 자동 입력 (수정 가능)
- 2, 3지망: 선택 입력
- 같은 교수 중복 선택 불가

### 3. 대체 강의 시스템
- 각 선택 강의당 최대 2개 대체 강의
- 대체 강의 우선순위:
  - 예) 1지망 강의 -> 대체1 -> 대체2 -> 2지망 강의
- 같은 분야 강의 우선 추천
  - 같은 이수구분 (전공필수 -> 전공필수/전공선택)
  - 같은 교과분야 우선
- 대체 강의도 선택 불가 처리

### 4. 학점 제한 검증
- 최소 학점: 10점
- 최대 학점: 21점
- 실시간 학점 계산 및 표시
- 제출 시 범위 검증
- 범위 초과 시 경고 메시지

### 5. 데이터 일관성
- 선택된 강의 중복 방지
- 트랜잭션으로 데이터 무결성 보장
- 동시성 제어 (Go의 mutex 활용)

### 6. CSV 데이터 처리
- 애플리케이션 시작 시 CSV 파일 파싱
- SQLite DB에 자동 적재
- 개설강좌.csv 업데이트 시 재시작으로 반영

## 보안 고려사항

### 1. 비밀번호 보안
- bcrypt 해싱 (cost factor 10)
- 평문 비밀번호 저장 금지
- 최초 로그인 시 비밀번호 설정

### 2. 세션 관리
- HTTP-only 쿠키 사용
- 세션 타임아웃 설정 (2시간)
- CSRF 토큰 (필요 시)

### 3. SQL Injection 방지
- Prepared Statement 사용
- 모든 쿼리 파라미터 바인딩

### 4. 입력 검증
- 학번 형식 검증
- 우선순위 범위 검증 (1~7)
- 학점 범위 검증 (10~21)
- XSS 방지 (HTML 이스케이핑)

## 데이터 백업 및 추출

### 설문 결과 추출 기능
```bash
# 설문 결과를 CSV로 추출
./survey-linux-amd64 --export-results results.csv

# 생성되는 CSV 형식:
# 학번, 이름, 전공, 우선순위, 과목코드, 과목명, 교수1지망, 교수2지망, 교수3지망, 대체강의여부, 대체우선순위
```

### 데이터베이스 백업
```bash
# SQLite DB 파일 백업
cp survey.db survey_backup_20260505.db
```

## 향후 확장 가능성

### 추가 기능 후보
1. 관리자 페이지
   - 전체 설문 현황 조회
   - 과목별 수요 통계
   - 학생별 제출 현황

2. 시간표 미리보기
   - 선택한 강의 시간표 시각화
   - 시간 충돌 감지

3. 이메일 알림
   - 설문 기한 알림
   - 제출 확인 이메일

4. 데이터 분석 대시보드
   - 과목별 수요 그래프
   - 교수별 선호도 통계

## 개발 일정

| 단계 | 기간 | 상태 |
|-----|-----|-----|
| Phase 1: 프로젝트 초기 설정 | 1일 | 대기 |
| Phase 2: 백엔드 API 개발 | 2-3일 | 대기 |
| Phase 3: 프론트엔드 개발 | 3-4일 | 대기 |
| Phase 4: 통합 및 테스트 | 1-2일 | 대기 |
| Phase 5: 배포 준비 | 1일 | 대기 |
| **전체 예상 기간** | **8-11일** | - |

## 성공 기준

1. ✅ 학생이 로그인하여 수강 희망 과목을 선택할 수 있다
2. ✅ 1~7지망까지 우선순위를 설정할 수 있다
3. ✅ 교수 지망(1~3지망)을 선택할 수 있다
4. ✅ 대체 강의를 2개까지 선택할 수 있다
5. ✅ 학점 제한(10~21점)이 적용된다
6. ✅ 설문을 제출하고 재수정할 수 있다
7. ✅ 단일 실행 파일로 배포 가능하다
8. ✅ 교수님 서버에 이식하여 즉시 실행 가능하다

## 참고 문서

- [기능기획서](reference/학생용프론트_기능기획서.pdf)
- [개설강좌 데이터](reference/개설강좌.csv)
- [디자인 참고 - 메인페이지](reference/design/메인페이지.png)
- [디자인 참고 - 팝업](reference/design/팝업.png)
